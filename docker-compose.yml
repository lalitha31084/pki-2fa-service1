services:
  app:
    build: .
    ports:
      - "8081:8080"  # <--- CHANGED HERE
    volumes:
      - seed-data:/data
      - cron-output:/cron
    environment:
      - TZ=UTC
    restart: unless-stopped

volumes:
  seed-data:
  cron-output: